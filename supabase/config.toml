# Supabase CLI — https://supabase.com/docs/guides/cli/config
# Legacy Paystack alias functions keep JWT verification off.
# We validate the user session inside the function instead.
[functions.pocketfi-init]
verify_jwt = false

[functions.pocketfi-webhook]
verify_jwt = false
